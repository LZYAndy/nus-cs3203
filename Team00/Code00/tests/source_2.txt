procedure while {
    assignStmt1 = procedure + if - while * read / call % print ;
    while (( if > while ) && ( read < call + print ) || !(procedure != 0 )) {
        if ( (if >= while) && ((read <= call) || (print == procedure))) then {
            a1 = b1+ 111111111111111111111111111111 ;
            if ( if < while ) then {
                print procedure ;
            } else {
                read if ;
            }
        } else {
            while ( while >= if ) {
                a2 = 0 ;
            }
        }
    }
    A55igNstmt2 = a2 ;
    read read ;
    print print ;
}