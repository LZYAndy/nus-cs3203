#include "PQLParser.h"

using namespace std;

/*! 
Parses and validates the query. If validation succeeds, returns the list of results.
@param query The user's query statement.
*/
list<string> pql_parse_query(std::string query) {
	list<string> results;

	return results;
}